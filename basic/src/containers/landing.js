import React from "react";
import Helmet from "react-helmet";

const Landing = () => (
  <div className="content">
    <Helmet>
      <title>React-</title>
    </Helmet>
    <div>
      <span className="route-not-found-header">Landing Page</span>
    </div>
  </div>
);

export default Landing;
